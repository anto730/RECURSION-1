#include <stdio.h>
int getsum(int n)
{
    int sum = 0;
    while (n != 0) {
        sum = sum + n % 10;
        n = n / 10;
    }
    return sum;
}

int main()
{
    int n;
    printf("enter the number :\n");
    scanf("%d",&n);
    printf(" %d ", getsum(n));
    return 0;
}
